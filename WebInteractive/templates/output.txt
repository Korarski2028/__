<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Output Files</title>
    <style>
        body {
            font-family: sans-serif;
            background-color: #f0f2f5;
            padding: 2rem;
        }
        .container {
            max-width: 1000px;
            margin: auto;
            background: white;
            padding: 2rem;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
        }
        h1 {
            text-align: center;
            margin-bottom: 1rem;
        }
        h2 {
            font-size: 1rem;
            color: #6c757d;
            text-align: center;
            margin-top: 0;
            margin-bottom: 2rem;
            word-break: break-all;
        }
        ul {
            list-style-type: none;
            padding: 0;
        }
        li {
            background: #f8f9fa;
            border: 1px solid #dee2e6;
            padding: 1rem;
            margin-bottom: 0.5rem;
            border-radius: 4px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        li.dir {
            background: #e9ecef;
        }
        a {
            text-decoration: none;
            color: #007bff;
            font-weight: 500;
        }
        a:hover {
            text-decoration: underline;
        }
        .download-link {
            font-weight: normal;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Files in Output Directory (please close Tab when done)</h1>
        <h2>Current Path: /{{ current_path }}</h2>

        <ul>
            {% if current_path %}
                <li class="dir">
                    <a href="{{ url_for('output_files', subpath=current_path.rsplit('/', 1)[0] if '/' in current_path else '') }}">
                        [..] Parent Directory
                    </a>
                </li>
            {% endif %}

            {% for dir in dirs %}
                <li class="dir">
                    <a href="{{ url_for('output_files', subpath=(current_path + '/' if current_path else '') + dir) }}">
                        [{{ dir }}]
                    </a>
                </li>
            {% endfor %}

            {% for file in files %}
                <li>
                    <span>{{ file }}</span>
                    <a href="{{ url_for('download_file', filepath=(current_path + '/' if current_path else '') + file) }}" class="download-link">Download</a>
                </li>
            {% endfor %}
        </ul>

        {% if not dirs and not files %}
            <p>No files or directories found in this path.</p>
        {% endif %}
    </div>
</body>
</html>