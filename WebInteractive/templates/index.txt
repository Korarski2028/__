<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Web Interactive Tool</title>
    <style>
        body {
            font-family: sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            min-height: 100vh;
            background-color: #f0f2f5;
        }
        .login-container {
            background: white;
            padding: 2rem;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
            text-align: center;
        }
        .top-logo {
            max-width: 150px;
            margin-bottom: 1rem;
        }
        .form-wrapper {
            display: flex;
            align-items: center;
            gap: 1rem;
        }
        .input-fields {
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }
        .input-group {
            display: flex;
            flex-direction: column;
            align-items: flex-start;
        }
        input {
            padding: 0.5rem;
            border: 1px solid #ccc;
            border-radius: 4px;
            width: 250px;
        }
        .side-logo {
            max-width: 64px; /* Adjust size as needed */
        }
        button {
            padding: 0.75rem;
            width: 100%;
            border: none;
            background-color: #007bff;
            color: white;
            border-radius: 4px;
            cursor: pointer;
            font-size: 1rem;
        }
        button:hover {
            background-color: #0056b3;
        }
        .flash-messages {
            list-style-type: none;
            padding: 0;
            margin-top: 1rem;
        }
        .flash-messages .error {
            color: #D8000C;
            background-color: #FFBABA;
            padding: 0.5rem;
            border-radius: 4px;
        }
        .flash-messages .success {
            color: #4F8A10;
            background-color: #DFF2BF;
            padding: 0.5rem;
            border-radius: 4px;
        }
    </style>
</head>
<body>

    <div class="login-container">
        <img src="{{ url_for('static', filename='logo.png') }}" alt="Top Logo" class="top-logo">
        
        <h2>Please add your abc\username and password</h2>

        {% with messages = get_flashed_messages(with_categories=true) %}
            {% if messages %}
                <ul class="flash-messages">
                {% for category, message in messages %}
                    <li class="{{ category }}">{{ message }}</li>
                {% endfor %}
                </ul>
            {% endif %}
        {% endwith %}

        <form method="POST">
            <div class="form-wrapper">
                <div class="input-fields">
                    <div class="input-group">
                        <label for="username">Username</label>
                        <input type="text" id="username" name="username" required>
                    </div>
                    <div class="input-group">
                        <label for="password">Password</label>
                        <input type="password" id="password" name="password" required>
                    </div>
                    <button type="submit">Login</button>
                </div>
                <img src="{{ url_for('static', filename='logo2.png') }}" alt="Side Logo" class="side-logo">
            </div>
        </form>
    </div>

</body>
</html>
